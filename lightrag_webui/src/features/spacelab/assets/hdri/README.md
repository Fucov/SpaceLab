# HDRI

Place authored or CC0 `.hdr` / `.exr` studio environments here. The current renderer uses Drei's generated/studio environment and multi-point studio lighting to avoid bundling unverified external HDR files.
