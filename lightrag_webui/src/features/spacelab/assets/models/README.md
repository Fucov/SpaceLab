# Models

The current high-fidelity module is built from self-authored parametric assemblies in `mainScreen/3d/models`.

Drop licensed `.glb` or `.gltf` files here when available, then wire them through `AssetModel` in `mainScreen/3d/models/AssetModel.tsx`.
